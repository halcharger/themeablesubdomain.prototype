@{
    var themeLayoutFile = Session["ThemeLayoutFile"] as string ?? string.Empty;
    Layout = (string.IsNullOrEmpty(themeLayoutFile) || !File.Exists(themeLayoutFile)) 
                ? "~/Views/Shared/_Layout.cshtml" 
                : themeLayoutFile;
}
